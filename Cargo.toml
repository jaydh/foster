[workspace]
members = [
    "crates/foster-core",
    "crates/foster-server",
    "crates/foster-testgen",
    "examples/counter",
]
# foster-client is wasm-only — build it separately with wasm-pack
exclude = ["crates/foster-client"]
resolver = "2"
